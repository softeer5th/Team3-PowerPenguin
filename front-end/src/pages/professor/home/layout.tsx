import { Outlet } from 'react-router';

const ProfessorHomeLayout = () => {
  return (
    <div>
      <h1>Professor Home Layout</h1>
      <Outlet />
    </div>
  );
};

export default ProfessorHomeLayout;
